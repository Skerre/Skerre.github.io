import { key_socioe_layer_fillopacity } from "./constants.js";

var global_states = {};
// global variable to store all states

window.global_states = global_states;

// init some initial vals;
global_states[key_socioe_layer_fillopacity] = 0.7;